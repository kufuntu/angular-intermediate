import { ContactListComponent } from './contact-list.component';

describe('ContactListComponent', () => {
  let component: ContactListComponent;

  beforeEach(() => {
    component = new ContactListComponent(null, null);
  });

  describe('onInit', () => {
    it('should initialize the contacts', () => {
      pending();
    });
  });
});
